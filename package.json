{
  "name": "ci-test-app",
  "version": "1.0.0",
  "description": "Petite application Node.js sans dépendance, utilisée comme dépôt de test pour valider un pipeline CI/CD (install, test, build, deploy).",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test",
    "build": "node scripts/build.js"
  },
  "engines": {
    "node": ">=18"
  }
}
